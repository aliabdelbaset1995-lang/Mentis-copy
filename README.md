ISEF Auth UI

Quick start

1) Install

```bash
npm install
```

2) Run the dev server

```bash
npm run dev
```

3) Build for production

```bash
npm run build && npm run preview
```

What’s inside

- React + Vite
- Framer Motion for delightful micro-interactions
- SCSS with glassmorphism + animated gradient blobs

Notes

- This is a front-end-only demo for a modern login/signup experience. Hook the `onSubmit` handler in `src/components/AuthCard.jsx` to your API/auth provider.

